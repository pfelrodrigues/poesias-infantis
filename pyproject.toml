[project]
name = "books"
version = "0.0.1"
description = "Coleção de livros remasterizados, com biblioteca bilíngue e EPUB."
readme = "README.md"
requires-python = ">=3.12,<3.13"
dependencies = [
    "pymupdf==1.28.2",
    "pillow==12.3.0",
    "pyyaml==6.0.3",
]

[project.optional-dependencies]
dev = []

[tool.uv]
package = false
